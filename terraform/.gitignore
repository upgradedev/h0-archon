# Terraform state & local artifacts
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!example.tfvars

# Lambda build output (regenerated by archive_file on every apply)
build/
