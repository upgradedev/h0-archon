# next-auth@5 (beta) declares a peer range that lags behind Next 16, so strict
# peer resolution would ERESOLVE. The integration is compatible (JWT strategy,
# no DB adapter); relax peer resolution so local `npm i` and CI `npm ci` agree.
legacy-peer-deps=true
